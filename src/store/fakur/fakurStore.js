/* eslint-disable no-undef */
// import axios from 'axios';
// import { setDecryptCookie } from '@/helper/setCookie.js';

const state = {
  fakur: []
};

const getters = {
  dataFakur: state => state.fakur,
};

const actions = {
  // async getInvoices({ commit }) {
  // },
};

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations,
}